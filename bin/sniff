#!/bin/bash
# -*- mode: bash; tab-width: 2; -*-
# vim: ts=2 sw=2 ft=bash noet

# $1 = code_dir

[[ -f ${1}/Gomfile ]] && exit 0
[[ -f ${1}/main.go ]] && exit 0
[[ `find ${1} -maxdepth 1 -name '*.go'` ]] && exit 0
exit 1
